Example: Using Dependencies (path dep)

This example has two packages: mathlib (a library) and app (which depends on it).

Build and run:

    cd examples/with_deps/app
    ../../../bin/pact run src/main.pact
