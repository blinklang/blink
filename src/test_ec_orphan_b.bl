import test_ec_orphan_a.{Ngx}

pub trait Bump {
    fn bump(self) -> Int
}

impl Bump for Ngx {
    fn bump(self) -> Int { self.a + 1 }
}

pub fn run() -> Int {
    let x = Ngx { a: 41 }
    x.bump()
}
