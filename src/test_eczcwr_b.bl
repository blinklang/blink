// eczcwr helper B: a DIFFERENT struct also named Ngx, with field `b` (distinct
// from helper A's `a`). See test_eczcwr_a.bl for the rule.
pub type Ngx { b: Int }

pub fn make_b() -> Ngx {
    Ngx { b: 2 }
}
