pub fn gct_run_discard[T](x: T, f: fn(T) -> (T, Int)) -> T {
    let (x2, _) = f(x)
    x2
}

pub fn gct_run_keep[T](x: T, f: fn(T) -> (T, Int)) -> Int {
    let (_, n) = f(x)
    n
}

pub fn gct_run_pair[T](x: T, f: fn(T) -> (T, Str)) -> Str {
    let (_, s) = f(x)
    s
}
