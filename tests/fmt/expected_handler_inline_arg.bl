effect MyEff {
    effect Op {
        fn doit() -> Int
    }
}

fn caller(h: Handler[MyEff]) -> Int {
    with h {
        myeff.doit()
    }
}

fn make() -> Int {
    caller(handler MyEff {
        fn doit() -> Int {
            let x = 1
            x + 1
        }
    })
}
