fn main() {
    let output = "case:bravo"
    let diag = "details here with extra context to make the line long"
    assert(output.contains("\"case\":\"bravo\""), "expected case:bravo in JSON failure record; {diag}")
}
