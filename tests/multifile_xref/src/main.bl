import store.{store_set}
import lookup.{lookup_get}

fn main() {
    store_set("a", 42)
    store_set("b", 7)
    let av = lookup_get("a")
    let bv = lookup_get("b")
    let cv = lookup_get("c")
    io.println("a={av}")
    io.println("b={bv}")
    io.println("c={cv}")
}
