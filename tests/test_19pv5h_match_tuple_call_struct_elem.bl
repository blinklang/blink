type P { x: Int y: Int }

fn mk() -> (P, Int) {
    (P { x: 10, y: 20 }, 5)
}

test "match tuple-returning-call with struct element (hnkrcz path)" {
    let r = match mk() {
        (p, n) => p.x + p.y + n
    }
    assert_eq(r, 35)
}
