import compile_test_helpers

test "unknown derive name emits E1112" {
    let src = "@derive(Display)\ntype Point \{ x: Int, y: Int }\nfn main() \{}\n"
    compile_test_helpers.expect_compile_error(src, "derive_unknown", "E1112")
}
