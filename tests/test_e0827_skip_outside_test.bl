// E0827 -- SkipOutsideTest (br pt1f33 / spec br cbk2wq / tktdnc)
//
// E0827 is the sole live code for skip() called outside a test body,
// emitted by the §2.20 test-only symbol fence in src/typecheck.bl and
// wired through the catalog / diag_explain.

import compile_test_helpers.{verify_catalog_entry}

test "blink explain E0827 returns the catalog entry" {
    verify_catalog_entry("E0827", "SkipOutsideTest")
}
