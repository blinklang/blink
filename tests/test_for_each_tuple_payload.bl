import std.testing.{for_each}
test "tuple_payload" {
    for_each([("ones", (1, 2, 3))], fn(case: (Int, Int, Int)) {
        let (a, b, c) = case
        assert_eq(a, 1)
        assert_eq(b, 2)
        assert_eq(c, 3)
    })
}
