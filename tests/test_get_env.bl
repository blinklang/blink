test "PATH env var is set" {
    let path = env.var("PATH") ?? "not set"
    assert_ne(path, "not set")
}

test "missing env var uses default" {
    let missing = env.var("__PACT_NONEXISTENT_VAR__") ?? "default"
    assert_eq(missing, "default")
}
