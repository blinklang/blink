type Point { x: Int y: Int }

type Box { pr: Tuple[Point, Int] }

test "match Tuple field whose element is a struct" {
    let b = Box { pr: (Point { x: 3, y: 4 }, 5) }
    let r = match b.pr {
        (p, n) => p.x + p.y + n
    }
    assert_eq(r, 12)
}
