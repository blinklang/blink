import compile_test_helpers

fn main() {
    let src = "fn main() \{\}"
    let out = compile_test_helpers.compile_and_capture(src, "_mcf")
    compile_test_helpers.expect_clean(out, "empty main compiles without trait contract errors — formatter multiline call regression check")
}
