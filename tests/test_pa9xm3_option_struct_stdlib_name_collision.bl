type Row { n: Int }

fn make() -> Option[Row] { Some(Row { n: 1 }) }

test "Option[Row] coexists with stdlib std.db.row.Row" {
    let r = make()
    match r {
        Some(x) => assert(x.n == 1)
        None => assert(false)
    }
}
