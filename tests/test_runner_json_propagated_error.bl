// btvqbf: when a test body propagates an Err via ?, the runner NDJSON
// output must include cause:"propagated_error" and an error subobject
// with message + error_type fields. This drives Phase 4 (runtime_test.h).
//
// FIXTURE NOTE: this is a placeholder that documents the expected schema;
// it must be turned into a real shell-out + JSON assertion once the runner
// emits the new fields. Left here intentionally failing as a TDD reminder.

test "runner NDJSON emits cause + error for propagated Err (placeholder)" {
    // Spec target:
    //   { "name": "...", "status": "failed", "cause": "propagated_error",
    //     "error": { "message": "boom", "error_type": "Str",
    //                "origin": { "file": "...", "line": N, "col": N } } }
    assert_eq("TODO_BTVQBF_RUNNER_NDJSON", "implemented")
}
